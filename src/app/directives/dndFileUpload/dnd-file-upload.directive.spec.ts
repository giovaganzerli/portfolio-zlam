import { DndFileUploadDirective } from './dnd-file-upload.directive';

describe('DndFileUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new DndFileUploadDirective();
    expect(directive).toBeTruthy();
  });
});
